import { Background, BackgroundVariant } from "reactflow";

export function BackgroundGrid() {
  return <Background color="rgba(148, 163, 184, 0.18)" gap={24} size={1.2} variant={BackgroundVariant.Dots} />;
}
